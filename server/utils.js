function createConnection()
{
    // return a connection object that can be used for making database queries
    return;
}

function salt(hash, user)
{
    // take a hash and salt it, to then store in the database
    let salt = generateSalt();
    let conn = createConnection();
    // Use connection to post salt to a user
    return;
}

function salt(hash, salt)
{
    
}

function verify(hash, user)
{
    
}

function generateSalt()
{

}